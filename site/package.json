{
  "name": "pwm-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Pratyabhijna World Model public site",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/prepare-data.mjs && astro dev",
    "build": "node scripts/prepare-data.mjs && astro build && node scripts/check-build.mjs",
    "preview": "astro preview",
    "prepare-data": "node scripts/prepare-data.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.4"
  }
}
